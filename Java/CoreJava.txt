
* Final : https://www.geeksforgeeks.org/final-keyword-java/
	- variable: consts (generally do uppercase with _ separators): var reference can't change but internal can change. e.g. final array: can add/remove elements.
		Assign value only once but can do later (init later i.e.)
	- method: no overrides
	- class: can't inherit

* Initializer blocks

* Transient: To avoid serialization

*** Java serializable class:
what does it mean: All members are primitives OR instances of Serializable class or marked transient (java won't try to serialize them i.e. class shud be prepared to re-init them)

*** Class Loader
WHat' the deal with this for reading files ?!!!